const p = n/2+3
const n = 5 * 4 * 6 + 2 + 200

fn fonction<k, l>() = ()
    a = [0; (k+l)*0]
    b = [1; k*(n+p)]

module mod(g[n+p*9]) = ()
    shared c = [1; n/p]
    no:
        k = c[0..n/p-1]
    until
