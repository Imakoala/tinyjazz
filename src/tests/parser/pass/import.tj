import "importedsimple.tj"
import "imported/path.tj"

fn fonction_test(a, b, c[5]) = (e, f, g[8*9/5])
    e = a 
    b = mux(1, 0, 1)    
    c = appel_test(a, b, c)
    (a, b, c) = (1, 0, 1)


module moduletest(a, b, c) = (d, e, f)
    shared (a, b, c, d, e, f, g) = ()
    shared (u, h, d) = [1; 8]
    (a, b, c) = coucou(h)
    automaton
        node nodetest
            a = [1; 9]
            b = not c
        transition 
            | default -> nodetest
