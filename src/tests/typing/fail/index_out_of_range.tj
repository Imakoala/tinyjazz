fn rec<n>() = (a)
    if (n > 0) then
        a = rec<n-1>()
    else 
        a = 1[8]
    end if

module main() = (b)
    shared b = ()
    automaton
        node n 
            b = rec<10>()
        transition