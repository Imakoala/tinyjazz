module main() = ()
    shared a = ()
    n: 
        use b = other_mod(a)
    until
//        |b -> p
    p:
        use c = third_mod(a)
    unless
//        |c -> n

module other_mod(a) = (b)
    shared b = ()
    n:
        b = 1
    unless

module third_mod(c) = (a)
    shared a = ()
    n:
        a = c
    unless
