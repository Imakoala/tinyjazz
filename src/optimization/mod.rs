/*
This file will contain all the optimisations done on the program.
Currently, there are only basic optimisation, which are mostly simplification of operation
of which one member is constant
*/
pub(crate) mod basic;
